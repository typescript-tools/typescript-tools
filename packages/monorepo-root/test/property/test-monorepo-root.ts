import { testProp, fc } from 'ava-fast-check'

/**
 * Library under test
 */

import { monorepoRoot } from '../../src/monorepo-root'

testProp.skip(
    'TODO: property-test monorepo-root',
    [
        // arbitraries
    ],
    (
        // test arguments
    ) => {
        // return assertion
    }, {
        verbose: true,
        numRuns: 100
    }
)
